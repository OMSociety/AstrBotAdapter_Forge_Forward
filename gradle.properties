# NeoForge 26.2 加载器专属属性（共享属性由 build.gradle apply 父目录 gradle.properties）

# 内存与构建行为
# ModDevGradle 会按工具链下载并运行 Java 25；关闭文件系统监视以避免在不允许创建
# 原生线程/句柄的环境里报 "Couldn't open current thread, error = 5" 而直接失败。
# 代理参数直接写进 jvmargs：依赖解析、NeoFormRuntime 及其工具子进程都在这些 JVM 里跑，
# 只靠 systemProp 不足以覆盖插件解析与 NFR 子进程。不需要代理时删掉 -D 四项即可。
org.gradle.jvmargs=-Xmx2G -Dfile.encoding=UTF-8 -Dhttp.proxyHost=127.0.0.1 -Dhttp.proxyPort=7897 -Dhttps.proxyHost=127.0.0.1 -Dhttps.proxyPort=7897 -Dhttp.nonProxyHosts=localhost|127.0.0.1
org.gradle.vfs.watch=false

# 依赖下载代理（同上，覆盖 systemProp 形式的读取路径）
systemProp.http.proxyHost=127.0.0.1
systemProp.http.proxyPort=7897
systemProp.https.proxyHost=127.0.0.1
systemProp.https.proxyPort=7897
systemProp.http.nonProxyHosts=localhost|127.0.0.1

# The Minecraft version must agree with the Neo version to get a valid artifact
minecraft_version=26.2
# The Minecraft version range can use any release version of Minecraft as bounds.
minecraft_version_range=[26.2,)
# The Neo version must agree with the Minecraft version to get a valid artifact
# 注意：26.2.0.87 的 userdev access transformer 中含一条失效条目
# （public net.minecraft.core.HolderSet$1 contents()Ljava/util/List;，该类在 MC 26.2 已不存在），
# 会让 :createMinecraftArtifacts 的 recompile 阶段必然失败；26.2.x 分支仍有此条目，port/26.3 已删除。
# 26.2.0.80 及更早版本没有这两条 HolderSet AT，可干净构建，故在此固定为 26.2.0.80。
neo_version=26.2.0.80
# The Neo version range can use any release version of Neo as bounds
neo_version_range=[26.2.0.80,)
# The loader version range can only use the major version of NeoForge/FML as bounds
loader_version_range=[4,)
